spring:
  profiles:
    active:
    - dev
    - log_default  #logging in default format
#    - log_container  #logging in json format    
  batch:
    job:
      enabled: false    
      
resilience4j.timelimiter:
  configs:
    default:
      timeoutDuration: 3s      